<div class="container">
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :song %>
    <%= f.input :comment %>
    <%= f.submit 'Add to Playlist', class: 'btn btn-gradient' %>
  <% end %>
</div>
