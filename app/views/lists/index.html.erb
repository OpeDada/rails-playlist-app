<div class="home-banner flex-column" style="background-image: url(<%= asset_path('home.jpg') %>)">
  <%= link_to "Create a new playlist 🎧", new_list_path, class: "btn btn-gradient-home" %>
</div>
<h3>My Playlists</h3>
<div class="card-container text-center">
  <% @lists.each do |list| %>
    <% if list.photo.attached? %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path list.photo.key, height: 300, width: 400, crop: :fill %>')">
        <p><%= link_to list.name, list_path(list) %></p>
      </div>
    <% end %>
  <% end %>
</div>
